[buildout]

extensions =
    mr.developer

extends =
    base.cfg
    sources.cfg

allow-picked-versions = true
update-versions-file = picked-versions.cfg

always-checkout = true
auto-checkout =
    org.bccvl.movelib

parts =
    GDAL
    pyramid-dev
    test
